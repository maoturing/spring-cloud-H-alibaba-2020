server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    # 配置nacos地址
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'